# context-assistant

Personal context assistant on OpenClaw / ZeroClaw.
Keeps task context between steps: request -> context -> reply -> edit.

Copy `config.example.toml` to `config.toml` and fill in keys.
